objects:
        strings;
        files;
        circular_arrays;
        threads;

allocators:
        linked_arenas;
        block;

functionalities:
        syscall_wrappers;
        dynamic_libraries injection;
        graphic library;
        terminal graphic system;

///////// function that should not be used by anything else than the lib will be static (except normature functions)
